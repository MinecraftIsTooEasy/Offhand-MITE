- 2026-02-28（第34次）：修复“物品从副手交互回主手后，第三人称副手仍显示持有空气（隐形物品）”。根因是 `MixinRenderPlayer` 仅在有副手物品时把 `heldItemLeft` 设为 1，但副手为空时未复位为 0，导致左手持物姿态残留。已在 `renderSpecials` 头部/尾部统一加入可渲染栈判定（`stack != null && stack.stackSize > 0 && stack.getItem() != null`），并在无效副手栈或关闭渲染策略时强制将 `modelBipedMain/modelArmor/modelArmorChestplate.heldItemLeft` 复位为 0。编译通过（`./gradlew compileJava -x test`）。
- 2026-02-28（第35次）：修复“进食/饮用结束时副手转换流程 NPE 崩溃（`InventoryPlayer.convertOneItem` 读空栈）”。根因是 `MixinEntityPlayer#convertOneOfHeldItem` 在副手使用状态下强制切到副手槽调用 `convertOneOfCurrentItem`，当目标槽已被清空时触发 `item_stack.stackSize` 空指针。已改为：仅在 `offhand$isOffhandItemInUse && itemInUse != null` 时拦截；先按引用在背包中定位 `itemInUse` 实际槽位，找不到再回退副手槽；若目标槽为空则直接 `setInventorySlotContents(targetSlot, createdItemStack)` 兜底，避免调用 `convertOneItem` 读空指针。编译通过（`./gradlew compileJava -x test`）。
- 2026-02-28（第36次）：修复“主手弓射箭误消耗副手工具耐久（副手交互往返后出现）”。根因是 `MixinEntityPlayer#tryDamageHeldItem` 仅依赖 `offhand$isOffhandItemInUse` 判定，状态残留时会把主手弓的耐久扣减错误重定向到副手槽。已增加实例级保护：仅当 `itemInUse` 非空且与副手槽当前 `ItemStack` 为同一实例时才走副手耐久分支，否则回退原版主手逻辑。编译通过（`./gradlew compileJava -x test`）。
- 2026-02-28（第37次）：实现主手/副手并行使用体系（面向“主手弓+副手剑格挡”）。改动包括：1）`MixinEntityPlayer` 新增副手独立 in-use 状态（`offhand$itemInUse/offhand$itemInUseCount`），在主手已使用时副手 `setHeldItemInUse` 走独立分支，避免覆盖主手 `itemInUse`；2）副手第一人称渲染读取独立 useCount，不再误用主手计数；3）`isBlocking` 支持副手独立格挡；4）`MixinMinecraft#offhand$shouldTryOffhand` 放开主手弓（`ItemBow`）触发副手右键；5）新增 `MixinDamage`，在格挡耐久结算中将 `getHeldItemStack` 重定向为副手格挡物品，避免主手弓被错误扣耐久。编译通过（`./gradlew compileJava -x test`）。
- 2026-02-28（第38次）：修复“主手工具格挡时副手弓无法放箭”。根因是并行使用态缺少副手独立 `onPlayerStoppedUsing` 释放链路，导致副手弓拉弓后松键不会触发放箭；同时主手 `clearItemInUse` 会错误影响副手独立状态。已在 `MixinEntityPlayer` 完成生命周期补齐：1）副手独立 useCount 每 tick 递减并在到期触发 `onItemUseFinish`；2）注入 `stopUsingItem(boolean)`，松键时切到副手槽执行副手 `onPlayerStoppedUsing`；3）`clearItemInUse` 增加来源捕获，仅在“清理的是副手主使用态”时才复位副手标记；4）`setHeldItemStack/convertOneOfHeldItem/tryDamageHeldItem` 改为按 `currentItem==offhandSlot` 路由，避免主手上下文误拦截。编译通过（`./gradlew compileJava -x test`）。
- 2026-02-28（第39次）：修复 `MixinEntityPlayer` 注入描述符错误导致启动崩溃。问题为 `@Inject(method = "stopUsingItem")` 实际命中无参重载 `stopUsingItem()V`，但处理器声明为 `(boolean, CallbackInfo)`，触发 `InvalidInjectionException`。已改为显式签名注入 `@Inject(method = "stopUsingItem()V", at = @At("HEAD"))`，并调整处理器参数为仅 `CallbackInfo`。编译通过（`./gradlew compileJava -x test`）。
